pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

rootProject.name = 'javarel'

include 'framework'
include 'framework:foundation'

include 'platform'
include 'platform:admin'
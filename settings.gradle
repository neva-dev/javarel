pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

rootProject.name = 'javarel'

include 'framework'
include 'framework:core'
include 'framework:sample'

include 'platform'
include 'platform:admin'